# Policy for irrc daemon
type irrc, domain;
type irrc_exec, exec_type, file_type;

# Started by init
init_daemon_domain(irrc)

# Allow access to /dev/msm_IRRC_pcm_dec
allow irrc irrc_device:chr_file rw_file_perms;
